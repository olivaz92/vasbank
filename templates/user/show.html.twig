{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ user.telephone }}</td>
            </tr>
            <tr>
                <th>Piece_identite</th>
                <td>{{ user.pieceIdentite }}</td>
            </tr>
            <tr>
                <th>Code_adhesion</th>
                <td>{{ user.codeAdhesion }}</td>
            </tr>
            <tr>
                <th>Code_parrain</th>
                <td>{{ user.codeParrain }}</td>
            </tr>
            <tr>
                <th>Niveaux</th>
                <td>{{ user.niveaux ? user.niveaux|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Compte_banquaire</th>
                <td>{{ user.compteBanquaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
