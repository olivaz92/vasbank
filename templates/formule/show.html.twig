{% extends 'base.html.twig' %}

{% block title %}Formule{% endblock %}

{% block body %}
    <h1>Formule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formule.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formule.nom }}</td>
            </tr>
            <tr>
                <th>Prix_adhesion</th>
                <td>{{ formule.prixAdhesion }}</td>
            </tr>
            <tr>
                <th>Montant_gagne</th>
                <td>{{ formule.montantGagne }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ formule.image }}</td>
            </tr>
        </tbody>
    </table>
    {{ form(payer) }}

    <a href="{{ path('formule_index') }}">back to list</a>

    <a href="{{ path('formule_edit', {'id': formule.id}) }}">edit</a>

    {{ include('formule/_delete_form.html.twig') }}
{% endblock %}
