{% extends 'base.html.twig' %}

{% block title %}Edit Formule{% endblock %}

{% block body %}
    <h1>Edit Formule</h1>

    {{ include('formule/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('formule_index') }}">back to list</a>

    {{ include('formule/_delete_form.html.twig') }}
{% endblock %}
